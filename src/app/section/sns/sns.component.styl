@import "../section.styl"

section

  margin-bottom 10vw

  .content

    max-width 400px
    
    ul
      display flex
      align-items center
      justify-content space-between
      list-style-type none
      margin 20px auto 0 auto

    a
      img
        width 100%
        max-width 48px
        height auto
